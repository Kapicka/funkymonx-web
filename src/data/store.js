
import {events} from "@/data/events";
import {music} from "@/data/music";
import {monxContact} from "@/data/contact";
import {iconLinks} from "@/data/iconLinks";
import {navigation} from "@/data/navigation";
import {photos} from "@/data/photos";
import {merchItems} from "@/data/merchItems";

const overlay = {visible: false}
const preloaderData = {visible: true, description: undefined}
const fixed = {value: false}
export {
    overlay,
    preloaderData,
    fixed,
    events,
    music,
    monxContact,
    iconLinks,
    navigation,
    photos,
    merchItems,
}
